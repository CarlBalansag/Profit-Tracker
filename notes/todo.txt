Change TopBar name to SummaryCards

Some TopCard should look at sold, some should look at when unsold
Sold
    profit
    net profit
unsold 
    Cashback
  
Transactions: CSV Export button does nothing

17. Transactions: Pagination is fake
"Page 1 of 1" and prev/next buttons are hardcoded. All rows always render.

18. Transactions: Inline sale edit cannot change commission fee or sale/payout dates
The salePayload on save doesn't include commission_fee, sale_date, or payout_date. If you need to correct a fee or date on a recorded sale, there's no way to do it inline.

19. Dashboard: "Generate Share Card" button does nothing
No onClick handler.


🟠 Data / DB structure issues
21. DB has no fees column on Inventory
If fees (e.g. eBay purchase fees, return fees) need to be tracked separately from shipping, there's no field for it.

22. DB has no order_number / tracking_number on Inventory
These are standard reselling workflow fields that are collected in the UI but have nowhere to go.

23. Platform.fee_pct is never used
The platform fee percentage exists on every Platform record but is never read in any calculation. Commission could be auto-calculated from it when recording a sale.

24. Buyer table exists but is never populated
buyer_alias in AddSale doesn't create or look up a Buyer. The Buyer table with avg_days_to_payout is designed but completely disconnected from the flow.

25. Dashboard dead code block ({false && ...})
Lines 411–514 are wrapped in {false && ...} — two large duplicate rendering blocks that never execute. They should be deleted.

The most impactful things to fix would be: fees/order_number/tracking being lost (add DB columns), AddSale profit preview not including tax/shipping, inline sale edit missing commission_fee, and the {false && ...} dead code. Want me to start fixing any of these?